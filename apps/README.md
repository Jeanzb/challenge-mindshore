# Apps

This monorepo keeps deployable applications under `apps/`.

- `backend`: ASP.NET Core Web API using Clean Architecture.
- `frontend`: React application built with Bun, TanStack Router, TanStack Query, Tailwind CSS, and shadcn/ui.

Shared root-level files such as Docker Compose, environment examples, and documentation stay at the repository root.
